#include "main.h"
#include <stdio.h>
/**
 * main - check the code
 * _isupper checks for upper case
 * Return: 1 if c is uppercase, 0 otherwise
 */
int _isupper(int c)
{
    return (c >= 'A' && c<= 'Z');
}